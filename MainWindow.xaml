<Window x:Class="SmartSearch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartSearch"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <GroupBox Header="Поиск">
                <TextBox x:Name="tbSearch" TextChanged="tbSearch_TextChanged"/>
            </GroupBox>
        </StackPanel>
        <DockPanel Grid.Row="1" Height="Auto">
            <ListBox x:Name="lbRecords"/>
        </DockPanel>
    </Grid>
</Window>
